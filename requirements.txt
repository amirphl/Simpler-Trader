# Core dependencies (optional - most functionality works without these)
plotly>=5.18.0  # For interactive plotting
kaleido>=0.2.1  # For exporting plots to PNG/SVG/PDF (optional)

# Web API
fastapi>=0.110.0
uvicorn[standard]>=0.30.0
requests>=2.31.0
psycopg[binary]>=3.2.0
psycopg_pool>=3.2.0

# Note: The core backtest and candle downloader modules use only Python standard library
# Plotly is only required if you want to generate visualizations
