# Live Trading Configuration Example
# Copy this file to live_trading.env and fill in your values

# Exchange Settings
EXCHANGE=weex  # Options: weex, binance, bybit
TRADING_MODE=futures  # Options: spot, futures (for exchanges that support both)
API_KEY=your_api_key_here
API_SECRET=your_api_secret_here
API_PASSPHRASE=your_api_passphrase_here  # Required for some exchanges (e.g., Weex)
TESTNET=true  # Set to false for live trading (WARNING: trades real money!)

# Trading Parameters
TIMEFRAME=15m  # Trading timeframe (1m, 5m, 15m, 1h, etc.)
TOP_M_SYMBOLS=100  # Number of top symbols to scan by volume
TOP_N_SIGNALS=5  # Number of top movers to trade
PRICE_CHANGE_THRESHOLD=2.0  # Minimum price change % (C parameter)

# Signal Parameters
HEIKEN_ASHI_CANDLES=3  # Number of HA candles before reversal (W parameter)
LEVERAGE=10  # Leverage multiplier (L parameter)
TAKE_PROFIT_PCT=1.0  # Take profit percentage (default: 1%)

# Position Management
MARGIN_MODE=isolated  # isolated or cross
DISABLE_SYMBOL_HOURS=24  # Hours to disable symbol after trade (B parameter, 0 = no disable)
POSITION_SIZE_USDT=100.0  # Position size in USDT
MAX_CONCURRENT_POSITIONS=5  # Maximum concurrent positions
MAX_POSITION_SIZE_PCT=10.0  # Maximum position size as % of balance

# Data Persistence
STATE_FILE=./data/live_trading_state.json
POSITIONS_DB=./data/live_trading_positions.db
LOG_FILE=./logs/live_trading.log

# Scheduling
CANDLE_READY_DELAY_SECONDS=30  # Delay after timeframe close to ensure candle availability

# Network (optional)
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
# PROXY=http://proxy.example.com:8080  # single proxy for both

# Notifications (optional)
# Set TELEGRAM_ENABLED=true to push executed trades to a Telegram chat/channel
TELEGRAM_ENABLED=false
# TELEGRAM_BOT_TOKEN=your_bot_token_here
# TELEGRAM_CHAT_ID=your_chat_id_here
# TELEGRAM_PROXY=http://proxy.example.com:8080
# TELEGRAM_TIMEOUT=10.0
